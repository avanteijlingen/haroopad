I love haroopad.

But i hate that it hasnt been updated in so many years and has dependencies on dead packages like gconf.

I want to refactor it to a modern code base but essentially keep all the same functionality.

In the end i still want to produce a windows .exe and a linux executable.

first scan the whole code base, then make a plan in plan.md

then execute the plan.

You can spawn as many agents as you like to get this done

do not stop working until the whole code base is refactored away from gconf and other dead dependencies and the whole thing can be compiled to a linux executable for this very PC im on.

Do not commit to github - I will handle that side of things manually.

You are allowed to modify .gitignore just not do your own commits.

